<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Storage Permissions for QR Code Download -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    
    <!-- Android 13+ (API 33+) Granular Media Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- Other Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Camera feature requirement -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/agrinext_ai"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/agrinext_ai_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgriNextAI"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">
        
        <!-- MainActivity - Main Dashboard -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- LoginActivity - User Authentication -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- RegisterActivity - User Registration -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- WeatherActivity - Weather Information -->
        <activity
            android:name=".activities.WeatherActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        
        <!-- AIAdvisorActivity - Plant Disease Detection -->
        <activity
            android:name=".activities.AIAdvisorActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        
        <!-- MarketPriceActivity - Crop Market Prices -->
        <activity
            android:name=".activities.MarketPriceActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        
        <!-- ChatbotActivity - Multilingual Assistant -->
        <activity
            android:name=".activities.ChatbotActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- TraceabilityActivity - QR Code Generation/Scanning -->
        <activity
            android:name=".activities.TraceabilityActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
            
        <!-- CreateHarvestCardActivity - Create New Harvest Cards -->
        <activity
            android:name=".activities.CreateHarvestCardActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
            
        <!-- HarvestCardDetailActivity - View Harvest Card Details -->
        <activity
            android:name=".activities.HarvestCardDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
            
        <!-- NGOSupportActivity - NGO Support Network -->
        <activity
            android:name=".activities.NGOSupportActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
            
        <!-- GovernmentSchemeActivity - Government Schemes and Subsidies -->
        <activity
            android:name=".activities.GovernmentSchemeActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
            
        <!-- SatelliteActivity - Satellite View and Farm Area Analysis -->
        <activity
            android:name=".activities.SatelliteActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
            
        <!-- AppIntroActivity - Interactive App Introduction -->
        <activity
            android:name=".activities.AppIntroActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AgriNextAI.NoActionBar" />
            
        <!-- CaptureActivity for ZXing QR Scanner -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation" />
        
        <!-- FileProvider for sharing images -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>

</manifest>
